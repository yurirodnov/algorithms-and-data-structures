#     Взять первый элемент массива и сравнить его со вторым. Если первый больше второго — поменять их местами с первым, если нет — ничего не делать. 1
#     Взять второй элемент массива и сравнить его со следующим — третьим. Если второй больше третьего — поменять их местами, если нет — ничего не делать. 1
#     Пройти так до предпоследнего элемента, сравнить его с последним и поставить наибольший из них в конец массива. 1
#     Вернуться в начало алгоритма и сделать всё снова точно так же, начиная с первого и второго элемента. Только теперь не проверять последний элемент — известно, что теперь в конце массива самый большой элемент. 1
#     Когда закончен очередной проход — уменьшить значение финальной позиции, до которой проверяют, и снова начать сначала. Так делать до тех пор, пока не останется один элемент. 1

# Принцип работы: на каждом шаге находится наибольший элемент из двух соседних и ставится этот элемент в конец пары. Получается, что при каждом прогоне цикла большие элементы будут всплывать к концу массива, как пузырьки воздуха — отсюда и название.